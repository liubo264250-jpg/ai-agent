<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubo.infrastructure.dao.IAiClientApiDao">

    <resultMap id="BaseResultMap" type="com.liubo.infrastructure.dao.po.AiClientApi">
        <id column="id" property="id"/>
        <result column="api_id" property="apiId"/>
        <result column="base_url" property="baseUrl"/>
        <result column="api_key" property="apiKey"/>
        <result column="completions_path" property="completionsPath"/>
        <result column="embeddings_path" property="embeddingsPath"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO ai_client_api (api_id, base_url, api_key, completions_path, embeddings_path, status, create_time, update_time)
        VALUES (#{apiId}, #{baseUrl}, #{apiKey}, #{completionsPath}, #{embeddingsPath}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT id, api_id, base_url, api_key, completions_path, embeddings_path, status, create_time, update_time
        FROM ai_client_api
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE ai_client_api
        SET api_id = #{apiId}, base_url = #{baseUrl}, api_key = #{apiKey}, completions_path = #{completionsPath},
            embeddings_path = #{embeddingsPath}, status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_client_api WHERE id = #{id}
    </delete>

    <update id="updateByApiId">
        UPDATE ai_client_api
        SET base_url = #{baseUrl}, api_key = #{apiKey}, completions_path = #{completionsPath},
            embeddings_path = #{embeddingsPath}, status = #{status}, update_time = #{updateTime}
        WHERE api_id = #{apiId}
    </update>

    <delete id="deleteByApiId">
        DELETE FROM ai_client_api WHERE api_id = #{apiId}
    </delete>

    <select id="queryByApiId" resultMap="BaseResultMap">
        SELECT id, api_id, base_url, api_key, completions_path, embeddings_path, status, create_time, update_time
        FROM ai_client_api
        WHERE api_id = #{apiId}
    </select>

    <select id="queryEnabledApis" resultMap="BaseResultMap">
        SELECT id, api_id, base_url, api_key, completions_path, embeddings_path, status, create_time, update_time
        FROM ai_client_api
        WHERE status = 1
        ORDER BY id
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT id, api_id, base_url, api_key, completions_path, embeddings_path, status, create_time, update_time
        FROM ai_client_api
        ORDER BY id
    </select>

    <select id="queryAiClientApiVOListByClientIds" resultType="com.liubo.domain.model.valobj.AiClientApiVO">
        SELECT DISTINCT
            a.id AS id,
            a.api_id AS apiId,
            a.base_url AS baseUrl,
            a.api_key AS apiKey,
            a.completions_path AS completionsPath,
            a.embeddings_path AS embeddingsPath
        FROM
            ai_client_config c
        JOIN
            ai_client_model m ON m.model_id = c.target_id AND m.status = 1
        JOIN
            ai_client_api a ON a.api_id = m.api_id AND a.status = 1
        WHERE
            c.source_type = 'client'
            AND c.target_type = 'model'
            AND c.status = 1
            AND c.source_id IN
        <foreach collection="clientIdList" item="clientId" open="(" separator="," close=")">
            #{clientId}
        </foreach>
    </select>
</mapper>
