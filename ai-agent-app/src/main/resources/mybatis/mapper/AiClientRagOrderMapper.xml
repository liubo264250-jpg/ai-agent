<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubo.infrastructure.dao.IAiClientRagOrderDao">

    <resultMap id="BaseResultMap" type="com.liubo.infrastructure.dao.po.AiClientRagOrder">
        <id column="id" property="id"/>
        <result column="rag_id" property="ragId"/>
        <result column="rag_name" property="ragName"/>
        <result column="knowledge_tag" property="knowledgeTag"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO ai_client_rag_order (rag_id, rag_name, knowledge_tag, status, create_time, update_time)
        VALUES (#{ragId}, #{ragName}, #{knowledgeTag}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT id, rag_id, rag_name, knowledge_tag, status, create_time, update_time
        FROM ai_client_rag_order
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE ai_client_rag_order
        SET rag_id = #{ragId}, rag_name = #{ragName}, knowledge_tag = #{knowledgeTag}, status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_client_rag_order WHERE id = #{id}
    </delete>

    <update id="updateByRagId">
        UPDATE ai_client_rag_order
        SET rag_name = #{ragName}, knowledge_tag = #{knowledgeTag}, status = #{status},
            update_time = #{updateTime}
        WHERE rag_id = #{ragId}
    </update>

    <delete id="deleteByRagId">
        DELETE FROM ai_client_rag_order WHERE rag_id = #{ragId}
    </delete>

    <select id="queryByRagId" resultMap="BaseResultMap">
        SELECT id, rag_id, rag_name, knowledge_tag, status, create_time, update_time
        FROM ai_client_rag_order
        WHERE rag_id = #{ragId}
    </select>

    <select id="queryEnabledRagOrders" resultMap="BaseResultMap">
        SELECT id, rag_id, rag_name, knowledge_tag, status, create_time, update_time
        FROM ai_client_rag_order
        WHERE status = 1
        ORDER BY id
    </select>

    <select id="queryByKnowledgeTag" resultMap="BaseResultMap">
        SELECT id, rag_id, rag_name, knowledge_tag, status, create_time, update_time
        FROM ai_client_rag_order
        WHERE knowledge_tag = #{knowledgeTag}
        ORDER BY id
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT id,rag_id, rag_name, knowledge_tag, status, create_time, update_time
        FROM ai_client_rag_order
        ORDER BY id
    </select>
</mapper>
