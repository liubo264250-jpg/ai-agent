<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubo.infrastructure.dao.IAiClientModelDao">

    <resultMap id="BaseResultMap" type="com.liubo.infrastructure.dao.po.AiClientModel">
        <id column="id" property="id"/>
        <result column="model_id" property="modelId"/>
        <result column="api_id" property="apiId"/>
        <result column="model_name" property="modelName"/>
        <result column="model_type" property="modelType"/>
        <result column="model_usage" property="modelUsage"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO ai_client_model (model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage)
        VALUES (#{modelId}, #{apiId}, #{modelName}, #{modelType}, #{status}, #{createTime}, #{updateTime},#{modelUsage})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE ai_client_model
        SET model_id = #{modelId}, api_id = #{apiId}, model_name = #{modelName}, model_type = #{modelType},
            status = #{status}, update_time = #{updateTime},model_usage = #{modelUsage}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_client_model WHERE id = #{id}
    </delete>

    <update id="updateByModelId">
        UPDATE ai_client_model
        SET api_id = #{apiId}, model_name = #{modelName}, model_type = #{modelType},
            status = #{status}, update_time = #{updateTime}, model_usage = #{modelUsage}
        WHERE model_id = #{modelId}
    </update>

    <delete id="deleteByModelId">
        DELETE FROM ai_client_model WHERE model_id = #{modelId}
    </delete>

    <select id="queryByModelId" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        WHERE model_id = #{modelId}
    </select>

    <select id="queryByApiId" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        WHERE api_id = #{apiId}
    </select>

    <select id="queryByModelType" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        WHERE model_type = #{modelType}
    </select>

    <select id="queryEnabledModels" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        WHERE status = 1
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time,model_usage
        FROM ai_client_model
        ORDER BY id
    </select>

    <select id="queryAiClientModelVOByClientIds" resultType="com.liubo.domain.model.valobj.AiClientModelVO">
        SELECT DISTINCT
            m.id,
            m.model_id AS modelId,
            m.api_id AS apiId,
            m.model_name AS modelName,
            m.model_type AS modelType,
            m.model_usage AS modelUsage
        FROM
            ai_client_config c
        JOIN
            ai_client_model m ON c.target_id = m.model_id AND m.status = 1
        WHERE
            c.source_type = 'client'
            AND c.target_type = 'model'
            AND c.status = 1
            AND c.source_id IN
        <foreach collection="clientIdList" item="clientId" open="(" separator="," close=")">
            #{clientId}
        </foreach>
    </select>
</mapper>
