<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubo.infrastructure.dao.IAiAgentFlowConfigDao">

    <resultMap id="BaseResultMap" type="com.liubo.infrastructure.dao.po.AiAgentFlowConfig">
        <id column="id" property="id"/>
        <result column="agent_id" property="agentId"/>
        <result column="client_id" property="clientId"/>
        <result column="sequence" property="sequence"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO ai_agent_flow_config (agent_id, client_id, sequence, create_time)
        VALUES (#{agentId}, #{clientId}, #{sequence}, #{createTime})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, agent_id, client_id, sequence, create_time
        FROM ai_agent_flow_config
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE ai_agent_flow_config
        SET agent_id = #{agentId}, client_id = #{clientId}, sequence = #{sequence}, create_time = #{createTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_agent_flow_config WHERE id = #{id}
    </delete>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, agent_id, client_id, sequence, create_time
        FROM ai_agent_flow_config
        ORDER BY id
    </select>
</mapper>
