<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubo.infrastructure.dao.IAiClientAdvisorDao">

    <resultMap id="BaseResultMap" type="com.liubo.infrastructure.dao.po.AiClientAdvisor">
        <id column="id" property="id"/>
        <result column="advisor_id" property="advisorId"/>
        <result column="advisor_name" property="advisorName"/>
        <result column="advisor_type" property="advisorType"/>
        <result column="order_num" property="orderNum"/>
        <result column="ext_param" property="extParam"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO ai_client_advisor (advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time)
        VALUES (#{advisorId}, #{advisorName}, #{advisorType}, #{orderNum}, #{extParam}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE ai_client_advisor
        SET advisor_id = #{advisorId}, advisor_name = #{advisorName}, advisor_type = #{advisorType}, order_num = #{orderNum},
            ext_param = #{extParam}, status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_client_advisor WHERE id = #{id}
    </delete>

    <update id="updateByAdvisorId">
        UPDATE ai_client_advisor
        SET advisor_name = #{advisorName}, advisor_type = #{advisorType}, order_num = #{orderNum},
            ext_param = #{extParam}, status = #{status}, update_time = #{updateTime}
        WHERE advisor_id = #{advisorId}
    </update>

    <delete id="deleteByAdvisorId">
        DELETE FROM ai_client_advisor WHERE advisor_id = #{advisorId}
    </delete>

    <select id="queryByAdvisorId" resultMap="BaseResultMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE advisor_id = #{advisorId}
    </select>

    <select id="queryByStatus" resultMap="BaseResultMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE status = #{status}
        ORDER BY id
    </select>

    <select id="queryByAdvisorType" resultMap="BaseResultMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE advisor_type = #{advisorType}
        ORDER BY id
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        ORDER BY id
    </select>

    <select id="queryAiClientAdvisorVOByClientIds"
            resultType="com.liubo.domain.model.valobj.AiClientAdvisorVO">
        SELECT DISTINCT
            c.id,
            c.advisor_id AS advisorId,
            c.advisor_name AS advisorName,
            c.advisor_type AS advisorType,
            c.order_num AS orderNum,
            c.ext_param AS extParam
        FROM
            ai_client_config a
        JOIN
            ai_client_advisor c ON a.target_id = c.advisor_id AND c.status = 1
        WHERE
            a.source_type = 'client'
            AND a.target_type = 'advisor'
            AND a.status = 1
            AND a.source_id IN
        <foreach collection="clientIdList" item="clientId" open="(" separator="," close=")">
            #{clientId}
        </foreach>
    </select>
</mapper>
